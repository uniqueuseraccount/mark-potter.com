apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextjs-dev-code
  namespace: mark-potter-dev
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 2Gi
